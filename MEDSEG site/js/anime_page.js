document.addEventListener('DOMContentLoaded', function() {
    AOS.init({
      duration: 1000, // Duração da animação em milisegundos
      once: true,     // Animará apenas uma vez ao descer a página
    });
  });